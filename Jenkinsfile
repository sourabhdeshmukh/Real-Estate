pipeline {
    agent {
        label 'docker-agent'
    }
    stages {
        stage('Get Source') {
            steps {
                sh 'ls'
                sh 'pwd'
            }
        }
    }
}
