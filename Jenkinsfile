pipeline {
    agent {
        docker { 
            label 'docker-agent'
         }
    }
    stages {
        stage('Test') {
            steps {
                sh 'Hello From Real Estate'
            }
        }
    }
}
